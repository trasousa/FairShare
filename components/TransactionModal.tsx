import React from 'react';

// This component is deprecated in favor of the MonthlyWorksheet view.
// Keeping file structure to prevent import errors during migration.
export const TransactionModal = () => {
  return null;
};